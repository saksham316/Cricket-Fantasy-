<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .field {
            margin-bottom: 20px;
        }

        .field label {
            display: block;
            font-weight: bold;
            color: #555;
            margin-bottom: 5px;
        }

        .field input[type="text"],
        .field input[type="email"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            background-color: #f9f9f9;
            /* Disable editing */
            pointer-events: none;
            /* Disable editing */
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>User Profile</h1>
        <div class="field">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" placeholder="Enter your name" value=<%=profile.name %> disabled>
        </div>
        <div class="field">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" placeholder="Enter your email" value=<%=profile.email %>
            disabled>
        </div>
        <div class="field">
            <label for="id">ID:</label>
            <input type="text" id="id" name="id" placeholder="Enter your ID" value=<%=profile._id %> disabled>
        </div>
    </div>
</body>

</html>